-- src/main.lua | Aurum
-- Bootstrap modular (no usado en bundle, pero documenta el orden de carga)
-- Ver docs/ARCHITECTURE.md para el flujo real

local Theme = require(script.Core.Theme)
local Storage = require(script.Core.Storage)
local Utils = require(script.Lib.Utils)
-- local Factory = require(script.UI.Factory)
-- local Components = require(script.UI.Components)
-- local Aim = require(script.Modules.Aim.AimAssist)
-- local ESP = require(script.Modules.Visuals.ESP)

print("[aurum] src/main.lua placeholder - el bundle es Aurum.lua")
